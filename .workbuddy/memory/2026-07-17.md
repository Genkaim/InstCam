# 2026-07-17 Picocam 工作日志

## 11:49 拍照过渡动画：与详情页尺寸对齐 + 线性打印 + 无圆角
- 用户三点：① 打印动画照片大小=相册详情照片大小（方便过渡）；② 照片无圆角、正常展示；③ 打印为线性，最初照片不可见，从灵动岛中间向下移出，再到画面中央。
- 关键事实：`PhotoViewerActivity` 照片布局为 `Modifier.fillMaxWidth().padding(24.dp)` + `ContentScale.Fit` + 居中（宽=屏−48dp，按比例 Fit）。
- 重写 `ui/components/CaptureTransitionOverlay.kt`：
  - 照片图层用与详情页**完全一致**的 `fillMaxWidth().padding(24.dp)` + Fit + `align(Center)`，去掉 background/RoundedCornerShape（无圆角）；用 `graphicsLayer.translationY` 做竖向位移，`translationY=0` 即与详情页位置/尺寸重合 → 无缝过渡。
  - "出纸口"用 `Modifier.drawWithContent { clipRect(top = revealLinePx) { drawContent() } }`，裁切线取灵动岛中心 `pillCenterY*density`；照片在线以上被裁掉（初始不可见），从下方露出（打印效果）。
  - 三阶段：morph 380ms（取景框→岛）；emit 650ms **LinearEasing**（ty 从 `-screenHpx`(隐藏) 线性到 `-screenHpx*0.16`(打印完成)）；settle 500ms（ty 从打印完成平移到 0=正中）→ `onDone`。去掉了之前的 alpha 淡出，改为尺寸一致的无缝落位。
  - 灵动岛外壳仍为独立 Box 画在最上层（不被 clip），盖住出纸口上方；宽仍屏宽 70%、高/圆角/上下位置用灵动岛设置。
- 新增 imports：`LinearEasing`、`drawWithContent`(ui.draw)、`clipRect`(ui.graphics.drawscope)、`graphicsLayer`、`fillMaxWidth`、`padding`；移除不再用的 `alpha`、`min`、`ContentScale` 相关旧字段/`cardW/cardH` 等。
- 编译：`assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk。

## 11:57 过渡动画：打印态照片缩小 + 打印放慢 + 放大到详情尺寸
- 用户反馈：上一版照片全程满屏、岛"放不下"；要求：① 整体小一点（打印态照片略小于灵动岛宽度，岛放得下）；② 打印动画慢一些；③ 照片全部出现后，放大到"照片模式大小"（详情页满屏）再进详情。
- 重写 `CaptureTransitionOverlay.kt`：
  - 打印态照片宽 `photoPrintW = pillW*0.85`（岛宽 70% 的 0.85，略小）；`photoFullW = screenW-48`（详情页满宽）；`scalePrint = photoPrintW/photoFullW`（clamp 0.2~1）。
  - 四阶段：morph 380ms（取景框→岛）；**emit 1100ms LinearEasing（放慢）**（scale 恒=scalePrint，ty 从 `-screenHpx`(隐藏) 线性到 `tyPrinted=(printedCenterY-屏幕中心)*density`，`printedCenterY = pillCenterY + screenH*0.34` 即岛口下方整张可见）；**grow 450ms**（scale 从 scalePrint→1、ty 从 tyPrinted→0，即放大到详情页满屏尺寸并移到正中）；→ `onDone`（此时与详情页完全重合）。
  - 放大阶段灵动岛外壳 `shellAlpha = 1-g` 淡出，交棒给照片。
  - 出纸口 clipRect 裁切线仍取灵动岛中心；照片无圆角、用详情页同布局。
- 编译：`assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk。

## 12:05 过渡动画：灵动岛拆上下两半 + 详情页渐入（不影响其他入口）
- 用户两点：① 打印阶段灵动岛拆成上下两半，上半遮住照片、照片遮住下半；② 照片 grow 后进入详情页要"渐入"，且不能影响相册等其他入口的默认动画。
- `ui/components/CaptureTransitionOverlay.kt`：
  - 灵动岛外壳拆为【下半】+【上半】两个 Box：下半先画（被照片盖）、照片居中画、上半最后画（在照片之上）。下半圆角仅 bottom、上半圆角仅 top（在中心相接处为方角，合并=完整药丸）；`shellAlpha = 1-g` 放大阶段淡出。
  - 照片 clipRect 裁切线改为取 morph 后的岛中心 `revealLinePx = (shellTop + shellH/2)*density`，保证打印口在上下两半之间。其余（photoPrintW=岛宽*0.85、emit 1100ms Linear、grow 450ms scale→1/ty→0）不变。
- `ui/CameraScreen.kt`：`onSelect` 签名由 `(File)->Unit` 改为 `(File, Boolean)->Unit`；相册 `GridPhotoCard` 调 `onSelect(file, false)`（默认动画）；过渡 `onDone` 调 `onSelect(file, true)`；启动 Intent 带 `fade_in` 标记。
- `PhotoViewerActivity.kt`：`onCreate` 读 `fade_in`，仅 true 时 `overridePendingTransition(android.R.anim.fade_in, android.R.anim.fade_out)` → 仅拍照过渡入口渐入，相册等其他入口走默认动画不受影响。
- 编译：`assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk。

## 12:22 过渡动画：打印更慢 + 缝露陷修复 + 放大后本页显示三按钮（不进详情页）
- 用户三点：① 打印动画再慢一点；② 取景框→灵动岛过渡时上下两半缝隙看到背景颜色（露陷）；③ 照片放大后**不进入**照片详情页，改为在本页照片下方直接放详情页底部三个按钮（保存/分享/删除）。
- `ui/components/CaptureTransitionOverlay.kt`：
  - emit 时长 1100→**1600ms**（更慢）。
  - 拆两半时两半在中心**重叠 1dp**（`seamOverlap`），消除接缝处的背景露陷；`shellH/2 + seamOverlap` 高度，下半 top 上移 `seamOverlap`。
  - clipRect 仅在"打印阶段(e<1)"生效（`then(if(e<1f) drawWithContent{clipRect...} else Modifier)`），放大完成后不再裁切 → 照片完整显示。
  - **放大完成后不再调 onDone 进详情页**：`grow` 结束置 `showDetail=true`，在 overlay 内显示顶部返回(ArrowBack→onExit) + 底部三按钮（保存=saveToGallery+Toast、分享=shareImage、删除=file.delete()后onExit），停留在当前页。根 Box 加 `background(Transparent).clickable{}` 阻挡点击穿透到相机界面。组件签名 `onDone:(File)->Unit` 改为 `onExit:()->Unit`；私有 `saveToGallery`/`shareImage` 从 PhotoViewer 复制复用。
- `ui/CameraScreen.kt`：过渡调用改为 `onExit = { animating=false; transitionFile=null }`（不再 `onSelect(file,true)`）。相册仍 `onSelect(file, false)` → 默认动画不受影响。
- 注意：`PhotoViewerActivity.kt` 的 `fade_in` 逻辑现在拍照路径用不到了（但保留无害，相册入口不受影响）。
- 编译：`assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk。

## 12:32–12:56 过渡动画：grow 后模糊背景 + 点背景/删除关闭 + 三按钮放照片下方（移除箭头）
- 用户三点：① 照片 grow 完成后，背景变为"全局背景色半透明 + 模糊"；② 关闭方式：点背景处 / 点删除（删除后退出），**左上角箭头去掉**；③ 保存/分享/删除三个按钮放在**照片正下方**（非页面底部），且圆角与相对定位遵循照片详情页样式（保存+分享=连通胶囊、删除=圆形）。
- `ui/components/CaptureTransitionOverlay.kt`：
  - 新增 `onEnterDetail: () -> Unit` 参数；`grow` 结束置 `showDetail=true` 并回调 `onEnterDetail()`。
  - showDetail 时叠加半透明全局背景 `Box(Modifier.fillMaxSize().background(AppBgColor.copy(alpha=0.6f)).clickable{onExit()})`；点击背景关闭。
  - 照片 `AsyncImage` 用 `onGloballyPositioned` 记录 `photoBottomPx`（用 `localToRoot(Offset.Zero).y + size.height`，因为本版本 Compose 已移除 `boundsInRoot`），按钮行 `offset{IntOffset(0, clampedY)}` 置于照片正下方（`clampedY = min(photoBottomPx + 12dp, screenH - btnRowH - 8dp)`）。
  - 按钮：`AnimatedVisibility(visible=showDetail, enter=fadeIn(tween(250))) modifier=fillMaxWidth()` 内 `Row`（去掉 `.align(TopCenter)`，因 AVC 内容非 BoxScope 会编译不过）→ `Row` 用 `offset + fillMaxWidth + clickable{}`（consume 防误关）；保存(128x56 左大圆角胶囊)/分享(右大圆角胶囊) 用 `RetroCream.copy(0.85)` 胶囊底，删除 56dp `CircleShape` `RetroRust.copy(0.15)`；保存=saveToGallery+Toast，分享=shareImage，删除=file.delete() 后 onExit。无箭头。
- `ui/CameraScreen.kt`：`CameraContent` 加 `var inDetail`；用 `Box(Modifier.fillMaxSize().then(if(inDetail) Modifier.graphicsLayer{ renderEffect = BlurEffect(...) } else Modifier))` 包裹相机内容层（模糊背景）。过渡调用：`onEnterDetail={inDetail=true}`、`onExit={inDetail=false; animating=false; transitionFile=null}`。
- 编译报错修复（**关键**）：
  1. 本 Compose 版本（BOM 2025.09.00）`graphicsLayer` 已从 `androidx.compose.ui.draw` **迁移到 `androidx.compose.ui.graphics`** → 两文件 import 改 `androidx.compose.ui.graphics.graphicsLayer`（`drawWithContent`/`clip` 仍在 `ui.draw`，不用动）。
  2. `BlurEffect` 构造函数收 `Float`(px) 而非 `Dp` → `BlurEffect(14f * density.density, 14f * density.density, TileMode.Decal)`（`density = LocalDensity.current`）。
  3. 模糊 Box 包裹 `Column` 后**少一个闭合大括号**导致 brace 结构错乱（连带 GridPhotoCard/Unresolved 级联）→ 在 Column 闭合后补一个 `}` 关模糊 Box。
  4. `fadeIn(250)` 类型错（收 animationSpec）→ `fadeIn(tween(250))`。
  5. `it.boundsInRoot` 在本版本已移除 → 改用 `coords.localToRoot(Offset.Zero)` + `coords.size.height`，新增 `import androidx.compose.ui.geometry.Offset`。
- 编译：`assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk（23.9MB）。
- 补强：模糊 `renderEffect` 仅在 Android 12+(API31, `Build.VERSION.SDK_INT >= S`) 应用，低版本仅保留半透明 scrim，避免 RenderEffect 在低版本 draw 时崩溃。

## 13:03 微调：打印更慢更宽 + grow 后背景更透/模糊更大
- 用户两点：① 打印动画再慢一点、打印态灵动岛（及照片）变宽一点；② grow 后背景透明度调低、模糊半径变大。
- `ui/components/CaptureTransitionOverlay.kt`：`pillW` 70%→**78%**（照片 `photoPrintW=pillW*0.85` 随之变宽）；`emit` 时长 1600→**2200ms**（更慢）；`scrimColor` alpha 0.6→**0.4**（更透明）。
- `ui/CameraScreen.kt`：模糊半径 `BlurEffect(14f*.., 14f*..)` → **`24f*density.density`**（更大模糊）。
- 编译：`assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk（13:03）。

## 13:12 微调：照片上移 + 背景淡入淡出过渡 + 遮罩更透明
- 用户三点：① grow 后照片（含下方三按钮）整体上移一些；② grow 后背景（遮罩+模糊） enter/exit 都要有过渡；③ 背景固有色透明度再降低。
- `ui/components/CaptureTransitionOverlay.kt`：
  - 新增 `detailUpPx = 60f * densityPx`；`ty` 在 grow 阶段终点由 `0` 改为 `-detailUpPx`（照片最终略偏上）；按钮 `desiredY` 同步减去 `detailUpPx`（按钮跟随上移）。
  - 遮罩由 `if(showDetail) Box` 改为 `AnimatedVisibility(enter=fadeIn(250), exit=fadeOut(250))`（淡入/淡出）；按钮 `AnimatedVisibility` 增加 `exit=fadeOut(250)`；照片 `graphicsLayer` 增加 `alpha = photoAlpha`（`animateFloatAsState(if(showDetail)1f else 0f)` → 关闭时淡出）。
  - 新增 `close()`：先 `showDetail=false`（遮罩/按钮/照片淡出）+ `onDetailState(false)`（模糊淡出），`delay(300)` 后再 `onExit()` 真正卸载（防动画被打断）。
  - 参数签名 `onEnterDetail: () -> Unit` → `onDetailState: (Boolean) -> Unit`（进入 true / 关闭 false）。背景点击、删除按钮均改调 `close()`。
  - `scrimColor` alpha 0.4 → **0.3**（更透明）。
  - 新增 imports：`fadeOut`、`animateFloatAsState`、`kotlinx.coroutines.delay`。
- `ui/CameraScreen.kt`：
  - 模糊改用 `animateFloatAsState` 驱动 `blurRadius`（0 ↔ 24px*density），`graphicsLayer{ renderEffect = BlurEffect(blurRadius, blurRadius, ...) }` 在 SDK>=S 常驻 → 模糊随 inDetail 淡入/淡出（替代原布尔瞬时切换）。
  - 过渡调用 `onEnterDetail={inDetail=true}` → `onDetailState={inDetail=it}`；`onExit` 仍置 `inDetail=false` 等清理。
  - 新增 import `androidx.compose.animation.core.animateFloatAsState`。
- 编译：`assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk（13:12）。

## 13:22 修复：打印/grow 照片不可见 + 照片 grow 后才入相册
- 用户两点：① 打印阶段和 grow 阶段的拍立得照片看不到（丢失）；② 在 grow 之后再把照片添加到相册。
- 根因①：上一轮给照片 `graphicsLayer{ alpha = photoAlpha }`，而 `photoAlpha` 绑定 `showDetail`（grow 完成前恒为 false）→ 打印/grow 全程 `alpha=0` 完全透明。改为新增 `closing` 标志，`photoAlpha = animateFloatAsState(if (closing) 0f else 1f)`：`closing` 仅在关闭淡出阶段为 true，打印/grow/详情展示期均为 1 → 照片全程可见，仅关闭时淡出。
- ② 相册加入时机：`CameraViewModel.takePhoto()` 的 `onImageSaved` 里原先立刻 `refreshPhotos()`（拍照即入相册，动画期间模糊背景里就会冒出缩略图）。改为去掉该处立即刷新，新增 `CaptureTransitionOverlay` 参数 `onAddToAlbum: () -> Unit`，在 `grow` 完成后（LaunchedEffect 内 `onDetailState(true)` 之后）调用 → `CameraScreen` 接线 `onAddToAlbum = { vm.refreshPhotos() }`，实现"grow 之后才加入相册"。
- 顺带：`onExit`（关闭/删除后）也加 `vm.refreshPhotos()`，使删除后相册及时同步。
- 编译：`assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk（13:22）。

## 13:33 微调：打印即时冒头且不超移 + 背景更透 + 按钮入场过渡
- 用户四点：① 进入打印后有一段时间照片才冒头（延迟）；② 照片尾部一出就算打印完，应直接 grow，不用再往下移；③ grow 后背景颜色只要"一点点透明"；④ grow 后三按钮出现也要有过渡。
- ①+② 根因：原 `ty = lerp(tyHidden=-screenHpx, tyPrinted=0.34屏高下移, e)`，起点太高（整张在屏幕外上方）→ 前段全被裁切，故"延迟冒头"；终点又远移到接近屏底 → "过头下移"。改为以出纸口（灵动岛中心 `revealLinePx`）为基准、按测量所得的照片缩放高度 `photoScaledHpx = (photoBottomPx-屏中心)*2 * scalePrint` 计算：`tyStart = revealLinePx - 屏中心 - photoScaledHpx/2`（底边刚在出纸口，整张在上方、被裁切 → 一进打印就立刻冒头，无延迟）；`tyEnd = revealLinePx - 屏中心 + photoScaledHpx/2`（顶边到出纸口，整张露出 → 打印完成，不再下移）。`ty = lerp(tyStart,tyEnd,e)`（打印）→ `lerp(tyEnd,-detailUpPx,g)`（grow）。删除 `tyHidden/printedCenterY/tyPrinted`。
- 测量时机修复：照片 `Box` 原在 `if(e>0.001||g>0.001)` 内才渲染 → 打印开始首帧 `photoBottomPx` 尚未测得，导致 `photoScaledHpx=0` 计算跳变/延迟。改为**始终渲染**照片 Box（打印前用 `alpha=0` + 出纸口裁切隐藏），`graphicsLayer{ alpha = (if(e>0.001||g>0.001)1f else 0f) * photoAlpha }`；morph 阶段即完成测量，emit 起始位置准确、无延迟无闪烁。新增 `import kotlin.math.max`。
- ③ 背景固有色透明度再降：`scrimColor` alpha `0.3 → 0.15`（更透，模糊更明显）。
- ④ 三按钮入场/出场过渡：`AnimatedVisibility enter = fadeIn(250) + slideInVertically(it/2)`、`exit = fadeOut(250) + slideOutVertically(it/2)`（淡入+上滑出现）。新增 `import slideInVertically/slideOutVertically`。
- 编译：`assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk（13:33）。

## 13:43 修复：焦段保持 + 去水波纹 + 按钮字体 + 先白闪
- 用户四点（其自行改过的参数不动）：① 拍照后焦段恢复 1x，应保持拍照前焦段；② grow 后点按有默认水波纹（indication）要去掉；③ 拍照后三按钮字体小一点；④ 拍照后先取景框白一下再做动画。
- ② 去水波纹：`CaptureTransitionOverlay` 所有 `.clickable { }` 改为 `.clickable(interactionSource = remember { MutableInteractionSource() }, indication = null) { ... }`（自带 `indication=null` 的便捷重载不存在，必须显式传 `interactionSource`）。新增 `import androidx.compose.foundation.interaction.MutableInteractionSource`。覆盖：根 Box（拦截穿透）、背景遮罩（关闭）、照片本身、按钮行整体、保存/分享/删除三个按钮。
- ③ 按钮字体：保存/分享 `Text` 的 `fontSize = 13.sp`（原更大）。删除按钮只图标无文字。
- ④ 先白闪：`CameraScreen` 的 `vm.photoCaptured.collect` 内改为 `if(!animating){ animating=true; delay(300L); transitionFile=file }`，让 `shutterFlash` 的 `tween(300)` 白闪先播完再弹出过渡层。新增 `import kotlinx.coroutines.delay`。
- ① 焦段保持（关键）：根因是 `transitionFile != null` 时原代码把 `ViewfinderFrame` 换成空 `Box` → `AndroidView` 内的 `PreviewView` 被卸载、`controller.bindToLifecycle` 重新触发，CameraX 重置 zoom 回 1x。改为**动画期间始终挂载 `ViewfinderFrame`**，仅 `modifier.alpha(if(transitionFile==null) 1f else 0f)` 隐藏（隐藏实时预览避免与灵动岛重影，但 PreviewView 不卸载、相机不重绑 → 焦段自然保留）；`flashAlpha` 在过渡期传 0f。并新增 `import androidx.compose.ui.draw.alpha`。防御性：过渡 `onExit` 增加 `vm.restoreZoom()`（重设回 `_zoomLevel`）确保退出后预览焦段准确。
- 注意：用户自己把 `CaptureTransitionOverlay` 的 `scrimColor` alpha 从 0.15 改成了 **0.87f**（更不透明），属其个人调试参数，本轮回均未触动。
- 编译：`assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk（13:43+）。

## 13:56 打印动画：出纸口下移到 80% + 真实打印机速度曲线
- 用户两点：① 灵动岛分割（出纸口）位置更靠下，约 80%；② 打印动画模拟真实打印机——先加速、再匀速、最后减速到 0（速度不突变），之后执行 grow。
- ① 出纸口位置：`CaptureTransitionOverlay` 新增 `seamFrac = 0.8f`、`seamYdp = shellTop + shellH*seamFrac`（dp）；`revealLinePx` 由原来取岛中心 `(shellTop + halfHpx)` 改为 `seamYdp`；两半几何随之参数化——上半（盖在照片之上、出纸口以上）高度 `shellH*seamFrac + seamOverlap`，下半（出纸口以下）高度 `shellH*(1-seamFrac) + seamOverlap`，接缝处仍重叠 `seamOverlap` 防露陷。删除不再使用的 `halfHpx`。
- ② 真实打印机缓动：新增文件级 `private val PrinterEasing = Easing { ... }`，实现梯形速度曲线——加速段 0.2、匀速段 0.5、减速段 0.3（占比常量），速度从 0 平滑升到恒定 `v=2/(1+const)` 再降到 0，C1 连续、总位移=1，无起步/收尾突变。`emit` 动画由 `LinearEasing` 改为 `PrinterEasing`；`import LinearEasing` 移除、`import Easing` 新增。morph/grow 仍用默认（fastOutSlowIn）缓动。
- 编译：`assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk（13:56）。

## 14:01 三项：出纸口回中间 + 摇一摇显色(陀螺仪) + 打印增速/减速缩短
- 用户三点：① 出纸口回到灵动岛中间（照片从中间出现）；② grow 后加"摇一摇显色"——获取陀螺仪数据，照片初始淡且低饱和，摇一摇后正常；③ 打印动画加速段/减速段持续时间减少。
- ① 出纸口：`seamFrac` 0.8 → **0.5**（回居中）；两半几何公式按 seamFrac 参数化，0.5 时即还原为上下各半的原始居中形态。
- ② 摇一摇显色（`CaptureTransitionOverlay`）：新增 `var revealed`（默认 false）、`val colorSat = animateFloatAsState(if(revealed)1f else 0.35f, 600ms)`。照片 `AsyncImage` 加 `colorFilter = if(colorSat<0.999f) ColorFilter.colorMatrix(ColorMatrix().apply{setToSaturation(colorSat)}) else null`（用 Coil 的 colorFilter 而非 renderEffect，跨 API 生效，避免 RenderEffect.createColorFilterEffect 在现 Compose 版本不可用）。陀螺仪监听：`DisposableEffect(showDetail, revealed)` —— 仅当 `showDetail && !revealed` 时注册 `SensorManager`；优先 `TYPE_GYROSCOPE`（阈值合成角速度>8 rad/s），无陀螺仪则退化 `TYPE_ACCELEROMETER`（阈值>18 m/s²）；检测到摇动置 `revealed=true` 并自动注销监听；关闭/退出时也注销。顶部加 `AnimatedVisibility(visible = showDetail && !revealed)` 提示"摇一摇，照片显色"（不拦截点击）。`LaunchedEffect(file)` 内 `revealed=false` 保证每张新照片都从"未显色"开始。新增 imports：Sensor/SensorEvent/SensorEventListener/SensorManager、DisposableEffect、ColorFilter/ColorMatrix、kotlin.math.abs。
- ③ 打印增速/减速缩短：`PrinterEasing` 三段占比 `accel 0.2/const 0.5/decel 0.3` → **`accel 0.15/const 0.7/decel 0.15`**（加速、减速各缩短，匀速加长；梯形速度曲线仍是先加速→匀速→减速至0，无突变）。
- 编译：`assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk（14:01）。

## 14:15 移除显色功能 + 打印增速/减速略微加长
- 用户两点：① 显色（摇一摇）功能先删掉；② 打印动画加速段/减速段持续时间略微增加。
- ① 移除全部显色相关代码：`revealed` 状态、`colorSat` 动画、`DisposableEffect` 陀螺仪/加速度计监听、`AsyncImage` 的 `colorFilter`、`AnimatedVisibility` 顶部"摇一摇，照片显色"提示、`LaunchedEffect` 内 `revealed=false` 重置；并删除不再使用的 imports（Sensor/SensorEvent/SensorEventListener/SensorManager、DisposableEffect、ColorFilter、ColorMatrix、kotlin.math.abs）。doc 注释 ⑤ 一并删除。
- ② 打印缓动 `PrinterEasing` 三段占比 `accel 0.15/const 0.7/decel 0.15` → **`accel 0.18/const 0.64/decel 0.18`**（加速、减速各略微加长，匀速相应缩短；仍是先加速→匀速→减速至0 梯形曲线）。
- 编译：`assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk（14:15）。

## 14:32 修复：出纸口偏置时灵动岛圆角不再是预设（改单一外壳+黑色遮罩）
- 问题：原实现把灵动岛拆成上下两个半块（各自只在外侧圆角、接缝处直角），出纸口 seamFrac 一旦偏置（如用户设 0.9）外壳就不再是预设圆角的完整灵动岛。
- 修法（按用户建议"用黑色元素在岛内遮住纸张"）：灵动岛外壳改为**单一圆角矩形** `Box(..., RoundedCornerShape(shellCorner.dp), background=IslandShellColor)`，绘制在照片**之前（底层）**，四角始终用预设 `shellCorner`。照片仍用 `clipRect(top = revealLinePx)` 只在出纸口以下露出；出纸口以上由黑色外壳遮住、以下照片透出 → 出纸口即一条直缝，外壳圆角恒为预设，任意 seamFrac 都正确。删除了上下两半 Box、`seamOverlap` 及相关注释；doc ②/③ 同步更新。
- 注意：用户自行把 `seamFrac` 设为 0.9、`emit` 时长改为 1700ms（缩短），均为其本地调试参数，本次未改动、保留现状。
- 编译：`assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk（14:32）。

## 14:46 修复：从设置返回主界面焦段变 1x
- 根因：`CameraViewModel.onPreviewStreaming()` 在预览重连（STREAMING）时只重设 FPS/对焦/快门（`pushCameraOptions`），**未还原焦段**；从设置返回使相机重新绑定，zoom 被重置为 1x 且无处恢复（`viewerLauncher` 返回有显式 `restoreZoom()`，但设置走的是 `startActivity` 无回调）。
- 修法（根因修复、覆盖所有重连路径）：`onPreviewStreaming()` 内新增 `controller.setZoomRatio(_zoomLevel.value)`，每次预览重连都按保存的 `_zoomLevel` 重新套用焦段。顺带也修了切摄像头后焦段可能被重置的潜在问题。`viewerLauncher`/`onExit` 里的显式 `restoreZoom()` 保留（无害、即时兜底）。
- 编译：`assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk（14:46）。

## 14:51 修复：从设置返回仍闪一下 1x 取景
- 问题（延续 14:46）：14:46 把焦段还原挪到 `onPreviewStreaming()`（STREAMING 回调）里，能回到正确倍率，但 STREAMING 在**首帧渲染之后**才触发→首帧仍以 1x 渲染，肉眼可见"瞬间 1x"。
- 根因确认：`SettingsActivity` 是**独立 Activity**（`CameraScreen.kt:236` `startActivity` 启动），进设置时 `MainActivity` 走 ON_PAUSE、`LifecycleCameraController` 解绑相机；返回时 ON_RESUME 重新绑定，绑定后**首帧即以 1x** 出流，原 STREAMING 回调晚于首帧。
- 修法：`ViewfinderFrame.kt` 新增 `DisposableEffect(lifecycleOwner, onPreviewStreaming)` 注册 `LifecycleEventObserver`，在 `Lifecycle.Event.ON_RESUME` 时抢先调用 `onPreviewStreaming()`（内含 `controller.setZoomRatio(_zoomLevel.value)`），从而**在相机重新绑定、首帧出流之前**就套好已保存焦段；原 `previewStreamState==STREAMING` 回调保留作兜底。新增 imports：`androidx.lifecycle.Lifecycle`、`androidx.lifecycle.LifecycleEventObserver`、`androidx.compose.runtime.DisposableEffect`。（注意：`PreviewView.StreamState` 本版本只有 `IDLE`/`STREAMING`，无 `STARTED`，故不能用 STARTED 判断。）
- 为啥 ON_RESUME 能在首帧前生效：相机首帧延迟远大于 ON_RESUME 内同步派发的 observer 链（controller 内部 observer 先触发重绑、紧接着我们的 observer 设焦段），无论排序先后，焦段都在首帧交付（STREAMING）前设好。
- 编译：`assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk（15:16 重建）。

## 15:33 硬战：取景框↔灵动岛 跟手展开/折叠 morph + 操控区两段动画 + 相册嵌套滚动协作
- 用户需求：取景框两态——(A)折叠=只剩灵动岛(位置/大小按设置)；(B)展开=取景框(按设置)。手指下滑随手把岛"跟手"展开成取景框，操控区随之出现；操控区分两段动画：先高度0→正常，再变不透明；所有动画全程跟手。反向(已展开时)：相册不在顶部→相册正常上滑；相册在顶部→折叠动画：取景框→岛、控制区先淡出再高度归0，同样跟手。
- 实现（全部在 `ui/CameraScreen.kt` 的 `CameraContent` 内）：
  - **进度状态** `progressState = mutableFloatStateOf(1f)`（1=展开/相机态，0=折叠/灵动岛）；`p = progressState.floatValue` 作为所有插值/动画的唯一驱动。
  - **跟手** `applyDrag(dy)`：`progress += dy/dragRangePx`（`dragRangePx=260*density`，即手指移动 260dp 走完 0↔1），clamp[0,1]，返回实际消费 px 供 nestedScroll 协作。`settleTo(velocity)`：松手用 `animate(cur,target,tween(260))` 吸附——速度>900→1、<-900→0，否则过半→1。
  - **相册嵌套滚动** `NestedScrollConnection`：`onPreScroll`（上滑 dy<0 且 p>0 且相册在顶部 `!gridState.canScrollBackward`→ 先折叠取景框，消费）；`onPostScroll`（下滑 dy>0 且 p<1，相册滚到顶后剩余量→展开）；`onPreFling`（progress 处于中途才 `settleTo(velocity.y)` 吸附并消费该 fling）。挂在 `LazyVerticalGrid.nestedScroll(nested)`。
  - **取景框/空相册本体拖拽** `headerDrag = pointerInput{ detectVerticalDragGestures(onVerticalDrag=applyDrag, onDragEnd/Cancel=settleTo(0f)) }`，挂在 `ViewfinderFrame` 与空相册占位 Box。
  - **几何插值 岛(p=0)↔取景框(p=1)**：取景框展开态方形（`vfW`，水平居中，`vfTop` 用设置页参考系）；灵动岛态 `isW/isH/isCenterX/isTop` 按 `diConfig`。逐帧 `fW=lerp(isW,vfW,p)`、`fH=lerp(isH,vfW,p)`（展开态高=vfW 方形）、`fTop=lerp(isTop,vfTop,p)`、`fCorner=lerp(岛角,取景框角,p)`、横向 `xOffsetPx=((fCenterX-vfCenterX)*density)` 用 `offset{IntOffset(xOffsetPx,0)}`。`borderDp=diConfig.heightDp`（折叠态黑边填满小取景框→呈现纯黑灵动岛）。**动画期间 PreviewView 始终挂载**（不卸载，避免焦段重置）。
  - **操控区两段动画**：`heightFactor=(p/0.5).coerceIn(0,1)`（前半段高度 0→1）、`ctrlAlpha=((p-0.5)/0.5).coerceIn(0,1)`（后半段透明度 0→1）；折叠时自动反向（先淡出再收高度）。外层 `Box.height(ctrlNaturalDp*heightFactor).clipToBounds()`，内层 `Column.wrapContentHeight(align=Top, unbounded=true).onSizeChanged{ctrlNaturalPx=it.height}.graphicsLayer{alpha=ctrlAlpha}`——unbounded 始终按自然高度测量（与裁切无关），`ctrlNaturalPx` 稳定，避免高度动画反馈抖动。相册区 `weight(1f)` 吸收全部高度变化（折叠时相册占满）。
- 新增 imports：`animate`(animation.core)、`detectVerticalDragGestures`、`offset`、`wrapContentHeight`、`mutableFloatStateOf`、`rememberCoroutineScope`、`clipToBounds`、`Offset`(ui.geometry)、`NestedScrollConnection/NestedScrollSource/nestedScroll`、`pointerInput`、`onSizeChanged`、`IntOffset`、`Velocity`、`kotlinx.coroutines.launch`、`kotlin.math.roundToInt`、`rememberLazyGridState`。
- 编译：`assembleDebug` BUILD SUCCESSFUL（首次即过，无编译错误），产物 app/build/outputs/apk/debug/app-debug.apk（15:33，约 23.9MB）。待真机验证跟手手感。

## 16:02 三个小修复：相册滑动掉帧 / 圆角过渡 / 相册顶部留空
- 用户三点反馈：① 底部相册滑动掉帧明显；② 灵动岛→取景框过渡时圆角也要过渡；③ 相册完全展开（折叠态）时，相册顶部距离灵动岛要留点空。
- ① 掉帧根因：相机内容层根 `Box` 始终挂 `graphicsLayer { renderEffect = BlurEffect(blurRadius,...) }`（SDK>=S）。即使 `blurRadius=0`，`renderEffect` 也会把整棵子树（含滚动中的相册 `LazyVerticalGrid`）每帧渲染到离屏 buffer 再合成 → 滚动掉帧。修法：新增 `blurActive = inDetail || blurRadius > 0.01f`，仅在 `blurActive` 时才挂该 `graphicsLayer`（详情态/模糊淡入淡出期间才激活）；普通展开态与相册滚动时 `blurActive=false` → 不挂 layer，滚动恢复顺滑。`blurActive` 在淡出结束(blurRadius→0)后才变 false，故模糊淡出仍完整。
- ② 圆角过渡：`ViewfinderFrame.cornerDp` 由 `Int` 改为 `Float`（默认 48f），`innerCorner` 的 `coerceAtLeast(10)` 改 `10f`；`CameraScreen` 传 `cornerDp = fCorner`（原 `fCorner.roundToInt()`）。改为连续 Float Dp 后，岛角↔取景框角在 morph 全程平滑过渡（不再被取整成阶梯）。`RoundedCornerShape(Float.dp)` 合法。
- ③ 相册顶部留空：在操控区 Box 与相册 Box 之间加 `Spacer(Modifier.height(8.dp))`。折叠态操控区高度=0 → 此 8dp 即灵动岛与相册间距；展开态为操控区与相册间距（网格自身还有 contentPadding 12dp）。
- 编译：`assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk（16:02，约 23.9MB）。

## 16:09 拍照后切换灵动岛(折叠)状态
- 用户需求：拍照之后的打印动画期间，相机 UI 要切换到灵动岛（折叠）状态；即拍完照后不再停留于展开取景框，而是收起成岛，符合"拍完回相册浏览"的直觉。
- 修法（`ui/CameraScreen.kt` 的 `photoCaptured` 收集处）：在 `transitionFile = file`（白闪 300ms 后）触发时，`scope.launch { animate(progressState.floatValue, 0f, tween(380)) { v,_ -> progressState.floatValue = v } }`，让取景框/操控区平滑收起（380ms，对齐 overlay 的 morph→岛 时长）。关闭照片详情(`onExit`)时不重置 progressState → 停留在灵动岛态（岛 + 相册占满）。
- 顺带把 `progressState` 声明从 `LaunchedEffect` 之后上移到该 `LaunchedEffect` 之前，避免前向引用潜在编译问题。

## 16:31 两个修复：相册顶部渐变 + 打印完成岛常驻(不消失)
- 用户两点：① 相册顶部加渐变（上=背景色、下=透明），仅相册展开(折叠/岛态)时出现；② 打印动画完成时灵动岛不要消失，而是留在设置位置、正常长度常驻；照片相比之前略微缩小+略微斜，下面仍是原三按钮，整体定位不变；加长版(打印态)用户称已自行实现、不动。
- ① 渐变（`ui/CameraScreen.kt` 相册 `Box` 内、网格之上）：新增 `albumTopFade = (1f - p).coerceIn(0,1)`，顶部 `Box(height 32.dp).graphicsLayer{alpha=albumTopFade}.background(Brush.verticalGradient(listOf(BgColor, Color.Transparent)))` 用 `align(TopCenter)`。折叠态(p=0)alpha=1 渐显；展开(p=1)alpha=0 消失；随手势跟手淡入淡出。需补 `import androidx.compose.ui.graphics.Brush`（原文件未导入，首编报 Unresolved reference 'Brush'）。
- ② 岛常驻（`ui/components/CaptureTransitionOverlay.kt`）：保留底部"打印机机身"岛（emit 期间为加长版视觉，g 期间 `shellAlpha=1-g` 淡出、被放大照片盖住）；新增**顶部常驻岛**绘制在照片之上，`alpha = g` 在 grow 期间淡入，与底部岛同位置/同尺寸(pillLeft/pillTop/pillW/pillH/pillCorner)无缝衔接 → 详情态岛不消失、为正常长度。照片：`scale` 终值 `lerp(scalePrint,1f,g)` → `lerp(scalePrint,0.92f,g)`（详情比满屏略小）；`graphicsLayer` 新增 `rotationZ = lerp(0f,3.5f,g)`（详情略微倾斜 3.5°）；`ty` 终值仍 `-detailUpPx`（整体定位不变）。三按钮保持原逻辑、在照片下方。
- 编译：`assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk（16:31，约 23.9MB）。
- 注意：overlay 自身仍绘制 morph→岛（真实 ViewfinderFrame 在 transitionFile 非空时 alpha=0 隐藏），二者视觉协同；底层 progressState→0 仅决定关闭详情后的终态为岛。

## 17:04 四处改动：打印岛过渡回设置长度 / 拍照隐藏效果区 / 打印位置在90%(加长岛) / 详情背景莫奈去色
- 用户四点：① 打印动画完后灵动岛长度要过渡回设置指定的长度(有过渡)；② 拍照后播放动画时要隐藏效果区(若展开)；③ bug：打印位置在灵动岛最下面而非 90%，需修复；④ grow 后的背景加莫奈去色(取色背景+90%不透明度)。
- ①+③ 打印态岛"加长版"：在 `CaptureTransitionOverlay.kt` 几何新增 `printH = max(pillW*0.5, 160f)`（竖直加长，使 90% 出纸口 seamFrac=0.9 成为清晰插槽而非贴底——修复③）；新增正常岛目标 `isW/isH/isCenterX/isCenterY`(取自 diConfig)。新增 `shrink = Animatable(0f)`，grow 完成后 `shrink.animateTo(1f, tween(320))`；顶部常驻岛改用 `lerp(pillW,isW,shrink)` 等，从加长版平滑过渡回设置的正常长度/高度/位置(修复①)。
- ② `ui/CameraScreen.kt` 的 `photoCaptured` 收集处加 `effectsExpanded = false`（拍照播放动画时隐藏效果区）。
- ④ 详情背景莫奈去色：复用项目已有的 `extractDominantColor`(Palette 取 LightMuted 主色，IO 线程小图解码；逻辑同 PhotoViewerActivity) 与新增 `desaturate`（向等亮度灰度混合 0.55 去色）；`LaunchedEffect(file)` 取色 → `animateColorAsState` 平滑过渡 → `scrimColor = monetColor.copy(alpha=0.9f)`。需补 import：`androidx.compose.animation.animateColorAsState`、`LinearEasing`、`mutableIntStateOf`、`android.graphics.BitmapFactory`、`android.graphics.Color as AndroidColor`、`androidx.palette.graphics.Palette`、`androidx.compose.ui.graphics.toArgb`(`toArgb` 是顶层扩展，须显式导入，首编报 Unresolved reference 'toArgb')。
- 编译：`assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk（17:04，约 23.9MB）。

## 17:18 修复：打印态灵动岛高度改回设置值 + 动画改为"morph→设置岛→横向加长→播放"
- 用户反馈：① 打印态灵动岛高度不正常、要按设置(diConfig.heightDp)，之前 `printH=max(pillW*0.5,160)` 竖直加长是错的；② 动画逻辑改为：取景框先 morph 成设置里的灵动岛大小 → 再横向变长(加长版) → 再播放打印动画。
- `CaptureTransitionOverlay.kt` 几何重构：删除 `printH` 竖直加长；打印态岛"加长版"改为**仅横向**(pillW=screen*0.78，以设置岛中心 isCenterX 为锚点左右对称加长 pillLeft)，高度/圆角/顶边( pillTop=isTop )完全沿用设置(isH/isCorner)。新增 `extend = Animatable(0f)`，`LaunchedEffect` 时序：morph(380)→extend(300)→emit(1700,PrinterEasing)→grow(450)→[onAddToAlbum]→shrink(320)。
- shell 几何：`shellH = lerp(fromH,isH,m)`（始终跟随 morph 到设置高度，打印态不加高）；`shellW`/`shellLeft` 用 when 链 (shrink>0→pillW..isW / extend>0→isW..pillW / 否则 morph 的 fromW..isW)；`shellTop` 仅 morph。顶部常驻岛 `piH=isH`、`piLeft=lerp(pillLeft,isLeft,s)`、`piTop=isTop`、`piCorner=isCorner`（去掉原 piH=lerp(printH,isH,s) 的竖直过渡）。seamFrac 仍为 0.9（出纸口位置不变）。
- 编译：`assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk（17:18，约 23.9MB）。

## 17:28 设置页分组重做 + 动画设置两个开关
- 需求：设置页按分组呈现——每组顶部靠左分组标题(留内容区内边距)；组内为一摞圆角矩形卡片，组内上下间距小(4dp)、组间间距大(30dp)；组内最上一张左上右上大圆角(28dp)、最下一张左下右下大圆角、其余小圆角(4dp)。第一组「灵动岛与取景框设置」= 已有的两个入口按钮；第二组「动画设置」= 两个开关。
- 新增 `dynamic/AnimPrefs.kt`：`AnimConfig(animEnabled=true, openViewfinderAfterCapture=false)` + `animStore` DataStore("anim_settings") + `Context.animConfig` Flow + `Context.saveAnimSettings{...}`(写盘并 `AppPrefs.updateAnimSettings`)。
- `dynamic/AppPrefs.kt`：新增 `_animSettings/_animSettings` 缓存流、`updateAnimSettings()`，`preload` 里加 `_animSettings.value = context.animConfig.first()`。
- `ui/CameraScreen.kt`：新增 `val anim by AppPrefs.animSettings.collectAsStateWithLifecycle()`。① 动画总开关关闭时 `photoCaptured` 收集里 `delay(300)` 后直接 `vm.refreshPhotos()` + `animating=false`，不显示 overlay、不折叠取景框（仅正常拍照）；② `onAddToAlbum` 里若 `anim.openViewfinderAfterCapture` 则 `animate(progressState 0→1, 380)` 把取景框展开到设置大小（grow 结束后触发；overlay 显示期间真实取景框 alpha=0 不可见，关闭详情后即呈展开态）。
- `SettingsActivity.kt`：重做 `SettingsContent` 为两个分组(GroupTitle + SettingsGroupColumn(spacedBy 4dp) + 卡片)；新增 `SettingToggleRow`(Switch + 支持 `enabled=false` 时文字变灰 RetroInk/RetroBrownLight alpha 0.35 且不可点击/不可拨动)。第二组：动画总开关(`anim.animEnabled`)；拍照后默认打开取景框(`anim.openViewfinderAfterCapture`, `enabled = anim.animEnabled` → 关闭时变灰仅总开关可操作)。写盘走 `context.saveAnimSettings{ copy(...) }`。需补 import：`ColumnScope`、`Switch`、`SwitchDefaults`、`collectAsStateWithLifecycle`、`rememberCoroutineScope`、`AnimConfig`、`AnimPrefs.saveAnimSettings`(首编报 Unresolved reference 'ColumnScope')。
- 编译：`assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk（17:28，约 23.9MB）。

## 17:58 三处修复：morph hold + 持久岛条件渲染 / 背景提前到 emit 后 / 照片飞入相册占位
- 用户三点：① morph(取景框→正常岛)时已有加长版岛覆盖，要先完全到正常岛再过渡到长版；② 背景过渡要在打印(emit)完成后即开始，不等 grow 完成；③ 退出预览后照片无缝飞入相册首位（grow 后首位放空白占位，退出时照片飞到该位置填入）。
- ① `CaptureTransitionOverlay.kt`：morph(380) 后加 `delay(200)` HOLD 让正常岛清晰可见，再 extend(300) 横向加长；持久岛改为 `if (g > 0.001f) { ... }` 条件渲染，morph/extend/emit 期间完全不组合(杜绝 alpha=0 仍可见)。
- ② `LaunchedEffect` 时序改为：morph(380)→delay(200)→extend(300)→emit(1700)→`showDetail=true;onDetailState(true)`[背景开始]→grow(450)→onAddToAlbum→shrink(320)。`showDetail` 从 grow 后移到 emit 后、grow 前。
- ③ 照片飞入相册：`CameraViewModel` 新增 `placeholderPhoto: StateFlow<File?>` + `setPlaceholder/clearPlaceholder`。`CameraScreen.kt` grid 中若 `placeholderPhoto==file` 则渲染空白占位(aspectRatio(1f)+onGloballyPositioned 测位置→`albumSlot:Rect?`)，传给 overlay。overlay 新增 `albumSlot` 参数 + `fly:Animatable(0→1)` + `flying/deleted` 标志：close() 时若 `!deleted && albumSlot!=null` 则 `fly.animateTo(1,tween(450))`（photo graphicsLayer 按 fly 插值 scale 0.92→缩略图、translation→albumSlot.center、rotation 3.5→0、alpha=1），否则 delay(300) 淡出。`onAddToAlbum` 里 `vm.setPlaceholder(file)`；`onExit` 里 `vm.clearPlaceholder()`。删除时 `deleted=true` 跳过飞入。
- 新增 photo 测量：`photoTopPx/photoLeftPx/photoWidthPx/photoHeightPx`（onGloballyPositioned 用 localToRoot 测全四边）。
- 编译：`assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk（17:58，约 23.9MB）。首编报 `Unresolved reference 'fly'`（fly 在 close() 之后声明），修复：fly 移到 photoAlpha 后、Animatable 块前。

## 18:06 设置页样式改进 + 个性化主题分组(深色/浅色/跟随系统) + 深色模式灰色描边
- 用户需求：① 分组标题不加粗、字体小、颜色淡；② 按钮下方小字不加粗；③ 按钮用安卓默认波纹；④ 新增"个性化"分组含主题颜色(深色/浅色/跟随系统)；⑤ 深色模式下取景框边框和灵动岛加灰色描边。
- 新增 `dynamic/ThemePrefs.kt`：`ThemeMode` 枚举(LIGHT/DARK/SYSTEM) + `ThemeConfig(mode)` + DataStore("theme_settings") + `Context.themeConfig` Flow + `Context.saveThemeSettings{...}`(写盘并 `AppPrefs.updateTheme`)。
- `dynamic/AppPrefs.kt`：新增 `_theme/_theme` 缓存流 + `updateTheme()`，`preload` 里加 `_theme.value = context.themeConfig.first()`。
- `SettingsActivity.kt`：① GroupTitle 改 `bodySmall`(12sp Normal) + `RetroBrownLight`（原 `titleMedium` Medium + RetroBrown）；② SettingRow/SettingToggleRow desc 改 `labelSmall.copy(fontWeight=FontWeight.Normal)`（原 labelSmall 是 Medium 半粗体）；③ 按钮 clickable 已含默认 ripple（未改）；④ 新增第三组"个性化"：`SettingRow("主题颜色","当前：${theme.mode.label}")` 点击弹 `AlertDialog`(RadioButton × 3)。移除"更多功能即将到来"。新增 import：AlertDialog/RadioButton/TextButton/FontWeight/remember/mutableStateOf/setValue/ThemeConfig/ThemeMode/ThemePrefs.saveThemeSettings。
- `ui/CameraScreen.kt`：新增 `val theme by AppPrefs.theme`；`isSystemDark` 按 `Configuration.UI_MODE_NIGHT_MASK`；`isDark = when(theme.mode){DARK→true/LIGHT→false/SYSTEM→isSystemDark}`。ViewfinderFrame modifier 加 `.then(if(isDark) Modifier.border(1.dp, Color.Gray, RoundedCornerShape(fCorner.dp)) else Modifier)`（在 .size 后 .alpha 前）→ 深色模式下取景框/灵动岛(同一 ViewfinderFrame 在 p=0) 多一层灰色描边。
- 编译：`assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk（18:06，约 24MB）。

## 18:15 两处修复：飞入相册定位 + 深色模式背景
- 用户两点 bug：① 照片飞回相册首位占位时略微偏上、定位不准；② 深色模式下所有背景应改深灰，且莫奈取色页背景叠加半透明灰色遮罩。
- ① 飞入定位（`CaptureTransitionOverlay.kt`）：飞入数学本身正确（graphicsLayer 以中心为旋转/缩放锚点，相对位移会抵消 `photoLayoutCy` 误差，最终落点恒为 `albumSlot.center`）。真正风险在 `albumSlot` 的取值时机——新增 `flySlot` 状态，在 `close()` 触发瞬间把当前 `albumSlot` 快照进去，飞入全程用 `flySlot` 作落点，避免 450ms 飞行途中因网格重布局导致目标漂移。另：`GridPhotoCard` 原本无 `aspectRatio`，与占位 cell（`.aspectRatio(1f)` 正方形）尺寸不一致，占位被真实照片替换时行高可能变化导致落点微移——给 `GridPhotoCard` 也加 `.aspectRatio(1f)` 保证占位与真实照片 cell 尺寸一致。
- ② 深色背景：新增 `ui/theme/Color.kt` 的 `RetroDarkBg=0xFF242424`（+ `RetroDarkSurface`）；新增 `dynamic/ThemePrefs.kt` 顶层 `isDarkMode(mode, isSystemDark)` 统一判定。（a）`CameraScreen` 主背景 / 相册顶部渐变改用 `if(isDark) RetroDarkBg else 原色`，并把 `isDark` 透传给 overlay；（b）`CaptureTransitionOverlay` 新增 `isDark` 参数：详情背景浅色=莫奈色(0.9)，深色=深灰底 `0xFF242424`(0.92) + 莫奈色淡显(0.4) + 半透明灰遮罩 `0xFF1A1A1A`(0.6) → 整体呈深灰；取色 fallback 在深色下用 `0xFF242424`；（c）`SettingsActivity` 背景 `if(isDark) RetroDarkBg else RetroPaper`；（d）`PhotoViewerActivity` 同样：深色=深灰底 + 莫奈淡显 + 灰遮罩。四处于深色模式均统一深灰，莫奈色作为"底纹"淡显而非刺眼原色。
- 编译：首轮报 2 类错——`0xFF242424` 是 Long 字面量、`mutableIntStateOf` 要 Int（改为 `0xFF242424.toInt()`）；`PhotoViewerActivity` 缺 `collectAsStateWithLifecycle` import（补）。修复后 `assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk（18:34）。
- 待真机验证：深色模式切到 DARK 后相机页/相册/设置/照片查看器背景是否均为深灰、莫奈页是否为"深灰+淡莫奈底纹"；飞入相册落点是否精确居中于首位。

## 18:46 三处修复：相册缩略图按照片比例 + 深色全白文字/图标 + 倍数区去背景
- 用户三点：① 每个相册元素预览照片的比例应和照片比例一样（之前强制正方形）；② 深色模式下所有文字和图标都变成白色；③ 切换画面倍数（缩放倍数条）区域去掉背景（无论深浅色）。
- ① 相册缩略图按真实比例（`ui/CameraScreen.kt`）：`GridPhotoCard` 与占位 cell 改用以 `SubcomposeAsyncImage` 读取 `painter.intrinsicSize` 计算 `ratio = w/h`，再 `Modifier.aspectRatio(ratio)` 显示，`contentScale = Crop` 填满该比例格（不再 letterbox/强制方形）。`GridPhotoCard` 用 `Image(painter=捕获的 painter, Crop)` 绘制（避免嵌套 BoxScope 与 SubcomposeAsyncImageContent 的 receiver 冲突，直接捕获 painter 到局部变量再画）。占位 cell 同样按 `ratio` 布局并 `onGloballyPositioned` 测位置供飞入落点 → 占位与真实照片 cell 同比例同尺寸，飞入落点精确。新增 import：`coil.compose.SubcomposeAsyncImage`、`androidx.compose.foundation.Image`（后移除未用的 SubcomposeAsyncImageContent）。
- ② 深色文字/图标全白：`ui/theme/Color.kt` 新增 `onSurface(isDark)`(深→白/浅→RetroBrown)、`onSurfaceSoft(isDark)`(深→0xFFCFCFCF/浅→RetroBrownLight)、`surfaceCard(isDark)`(深→RetroDarkSurface/浅→RetroCream)。`isDark` 透传至 `ControlBar`/`ZoomBar`/`CameraParamItem`（图标 tint、文案色用 onSurface/onSurfaceSoft；`CameraParamItem` 的 name/value/滑动提示图标色随 isDark 变白）、`SettingsActivity`（GroupTitle/SettingRow/SettingToggleRow 标题与 desc、ChevronRight 箭头、返回箭头、卡片背景 surfaceCard、主题对话框标题/选项/确认文字均随 isDark 变白/深灰卡片；Switch 的 rust/cream 配色保留）、`CameraScreen` 空相册文案与 `PermissionRequest`（标题/正文/授权按钮随 isDark 变白、授权卡片 surfaceCard）。`PhotoViewerActivity` 此前已用 `textColorForBg` 在深底上自动取白（无需改）。`CaptureTransitionOverlay` 详情页胶囊文字（RetroBrown on cream 胶囊）保持不动（深灰遮罩上 cream 胶囊仍清晰）。特效面板 `EffectsPanel` 自带浅色面板背景，内部文字保持深棕（不强制白，否则在浅面板上不可读）。
- ③ 倍数区去背景（`ControlBar.kt` 的 `ZoomBar`）：删除外层 `Box(clip(RoundedCornerShape(22.dp)).background(RetroCream.copy(0.6f)).padding(...))`，仅保留内层 Row；激活态仍是 rust 圆 + cream 文字，非激活文字用 `onSurface(isDark)`（深→白）。深浅色都无背景。
- 编译坑：首轮 `SubcomposeAsyncImage` 内容 lambda 把 `state` 当参数（实际是 receiver，`painter` 为 `SubcomposeAsyncImageScope` 的 receiver 属性，无 state 参数）→ 改为 `painter?.intrinsicSize`；次轮 `SubcomposeAsyncImageContent` 在嵌套 `Box` 内因 implicit receiver 变成 BoxScope 无法解析 → 改为捕获 `painter` 后用 `Image(painter=..., Crop)`。`onSurface` 在 CameraScreen 漏 import（补）。修复后 `assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk（18:46，约 24MB）。
- 待真机验证：① 相册缩略图是否严格按各照片自身比例显示（非方形）；② 深色模式下相机控件图标、设置页文字/图标、空相册与权限页文案是否均为白色（卡片为深灰表面）；③ 缩放倍数条是否无背景。

## 19:06 一处细节批：深色相册遮罩 + 详情/过渡按钮灰底 + 两设置页深色 + 设置分组标题细化 + Developer 栏
- 用户多需求：① 深色模式相册每张照片叠浅灰遮罩；② 深色模式照片详情页——左上返回灰底白图标、顶部文字白、底部按钮灰；③ 拍完动画后底部三按钮深色也改灰；④ 深色模式"灵动岛/取景框设置"两页适配；⑤ 开屏引导页适配（注：项目无独立 onboarding Activity，首项即 CameraScreen 的 PermissionRequest 权限引导，此前已深色适配）；⑥ 设置内每组标题字重改细、组间距变小；⑦ 个性化下"主题颜色"按钮四个角全大圆角；⑧ 设置底部加 Developer 栏（三行：Developer / Genkaim / zczzzzz，头像预留圆形占位）。
- ① `ui/CameraScreen.kt` 的 `GridPhotoCard`：在 `Image` 之后加 `if(isDark) Box(Modifier.fillMaxSize().background(Color(0x1AFFFFFF)))` 浅灰遮罩。
- ② `PhotoViewerActivity.kt`：新增 `graySurface=RetroDarkSurface`、`whiteOnDark=Color.White`；左上返回背景 `if(isDark) graySurface else (原)`、图标 `if(isDark) whiteOnDark else fgColor`；顶部时间/位置文本 `if(isDark) whiteOnDark else fgColor`；底部胶囊 `capsuleBg = if(isDark) graySurface else (原)`、图标文字白；删除圆 `if(isDark) graySurface else RetroRust(0.15)`、图标白。新增 import `RetroDarkSurface`。
- ③ `ui/components/CaptureTransitionOverlay.kt`：新增 `capsuleBg = if(isDark) RetroDarkSurface else RetroCream(0.85)`、`btnIconColor = if(isDark) White else RetroBrown`、`btnDelBg/btnDelIcon` 同逻辑；三按钮图标/文字/删除按此取色。新增 import `RetroDarkSurface`。
- ④ `dynamic/DynamicIslandSettingsActivity.kt` + `ViewfinderSettingsActivity.kt`：两页加深色适配——`isDark` 判定（AppPrefs.theme + isDarkMode）；外层背景 `if(isDark) RetroDarkBg else RetroPaper`、底部面板 `if(isDark) RetroDarkSurface else RetroCream`、背返回 `surfaceCard`+`onSurface` 图标、标题/正文 `onSurface`、重置按钮深 `RetroRust`、滑块 thumb/track 深 `White`/`onSurfaceSoft`、对话框文字 `onSurface/onSurfaceSoft`；`SettingSlider` 加 `isDark` 参数。新增 import：Configuration、isDarkMode、onSurface/onSurfaceSoft/surfaceCard、RetroDarkBg/RetroDarkSurface（及其 RetroRust）。Viewfinder 取景框示意黑框保留不变。
- ⑥⑦⑧ `SettingsActivity.kt`：GroupTitle 改 `bodySmall.copy(fontWeight=FontWeight.Light)`（更细）；组间 Spacer 30dp→16dp；SettingRow 新增 `allCorners` 参数（RoundedCornerShape(28.dp)），"主题颜色"行改用 `allCorners=true`（四角大圆角，替代原 top+bottom）；底部新增第四组"Developer"：GroupTitle + 卡片(RoundedCornerShape(28dp), surfaceCard) 内含圆形头像占位(56dp, onSurfaceSoft 0.25 底) + 两行文字 Genkaim / zczzzzz。新增 import `CircleShape`、`width`。
- 编译坑：首轮 `SettingsActivity` 缺 `width` import、`DynamicIslandSettingsActivity` 缺 `RetroRust` import（重置按钮深用 Rust）→ 补 import 后 `assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk（19:06，约 23.9MB）。

## 19:27 细节改动：相册顶距 + 主题内联子菜单 + 效果区大改
- 相册：LazyVerticalGrid contentPadding top 12dp→44dp，避免第一排被顶部 32dp 渐变遮挡。
- 设置-主题颜色：移除 AlertDialog 二级菜单，改为 `主题颜色` 行内右侧展开小菜单（ThemeColorRow），列出 浅色/深色/跟随系统 三个可点选 pill，深色/浅色自适应。
- 效果区大改（EffectsPanel + CameraViewModel）：
  - 新增 `TintState`（NONE/WARM/COOL）枚举 + `tintState` 状态；新增 `cycleTint`/`adjustTint`/`resetAll`，删除 `setTintState`（与 `var tintState` 的 setter 冲突）。`effective` 改为由 `tintState` 驱动色温（与旧暖/冷 enabled 互斥逻辑解耦），DataStore 新增 `tint_state` 键并兼容旧版按 enabled 推断。
  - 左侧调色盘默认常显：无滤镜=中性灰（拖动自动切暖色），暖/冷=对应渐变；下侧强度滑块常显。
  - 右侧仅保留 黑白/暗角/亮度（滑块+开关，功能不变）；删去原暖/冷两项。
  - 底部新增 `TintSelector`：居中胶囊，显示 无滤镜/暖色/冷色，左右箭头循环切换三态，替代原暖/冷滤镜。
- 编译：`assembleDebug` BUILD SUCCESSFUL，产物 app-debug.apk（23M）。

## 19:34 引导页与灵动岛/取景框设置页导航重构
- 新增持久化标记 `ThemeConfig.onboarded`（theme_settings DataStore 加 `onboarded` 布尔键，默认 false）。
- CameraScreen：权限页两个授权按钮改药丸型（`RoundedCornerShape(percent=50)`）；授权完成且未 onboarded 时，启动 `DynamicIslandSettingsActivity` 并立即在内存+DataStore 标记 onboarded，避免返回卡占位。
- DynamicIslandSettingsActivity：移除左上返回，头部标题左置、右上加圆形箭头（`ArrowForward`），深色 RetroDarkSurface 灰底白图标/浅色 RetroBrown 深底米色图标，与原返回同高；箭头以 `registerForActivityResult` 启动 ViewfinderSettingsActivity，返回结果后 `finish()`（连本页一起关）。
- ViewfinderSettingsActivity：移除左上返回，头部标题左置、右上加药丸型「完成」按钮（`RoundedCornerShape(percent=50)`），深色 `Color(0xFF3A3A3A)` 浅灰底白字/浅色 RetroBrown 深底米色字；点按 `setResult(RESULT_OK)+finish()`，结束整个引导栈回到主界面。
- 编译通过：`assembleDebug` BUILD SUCCESSFUL，产物 app-debug.apk（23M，19:34）。

## 19:45 分享去位置 + 相册详情页放大/滑动/缩略图
- 新增 `PhotoStorage.copyWithoutLocation(context, src)`：拷贝到 cacheDir 并清空全部 GPS EXIF 标签（已在 share_paths.xml 授权 cache-path）。
- 两处分享均改分享去位置副本：`PhotoViewerActivity.shareImage` 与 `CaptureTransitionOverlay.shareImage` 均改为 `suspend`，内部 `withContext(Dispatchers.IO){ copyWithoutLocation }` 后分享缓存副本（调用点包 `scope.launch`）。
- PhotoViewerActivity 重构：用 `HorizontalPager` 左右滑动切换照片（读取 `PhotoStorage.listPhotos` 倒序，启动定位到当前照片）；新增 `ZoomablePhoto`（transformable + graphicsLayer，1x~4x 捏合缩放、单指平移，放大时通过 isZoomed 禁用 Pager 翻页）；底部三个按钮上方加 `LazyRow` 相邻照片缩略图（当前高亮白边、点击跳转、随页自动居中）。保存/分享/删除均作用于 currentFile。
- 编译通过：`assembleDebug` BUILD SUCCESSFUL，产物 app-debug.apk（24M，19:45）。

## 19:50 设置页 Developer 栏重写 + 反馈按钮
- Developer 栏重写：去掉每元素卡片背景，拆为两行（无背景）：①作者 Genkaim（左侧圆形头像预留占位 + 右侧 GitHub logo 矢量绘制 `GithubLogo`，`PathParser` 解析 GitHub mark 路径，点击 `openLink` 跳转 `GITHUB_URL` 预留）；②特别鸣谢 / Zczzzzz（头像预留，底部对齐）。
- 主题颜色行下方新增「反馈与Bug提交」大圆角按钮（`RoundedCornerShape(28.dp)`，surfaceCard 背景，点击 `openLink` 跳转 `FEEDBACK_URL` 预留），与主题行间隔 14dp、与 Developer 组间隔 16dp。
- 链接均预留空串常量（`GITHUB_URL`/`FEEDBACK_URL`），非空白才跳转；新增 `openLink(context,url)` 辅助（`Intent.ACTION_VIEW`+`Uri.parse`，异常吞掉）。
- 编译通过：`assembleDebug` BUILD SUCCESSFUL，产物 app-debug.apk（23M，19:50）。

## 19:54 修复：引导触发点移出相机页
- 严重 bug：相机页用 `LaunchedEffect(Unit)` 在 `hasCamPerm && hasLocPerm && !onboarded` 时自动启动 `DynamicIslandSettingsActivity`，导致每次进相机都触发灵动岛/取景框设置。
- 修复：相机页改为仅在 `hasCamPerm && hasLocPerm && theme.onboarded` 时显示 `CameraContent`；否则均走 `PermissionRequest`。新增 `onNext` 回调（药丸型「下一步」按钮，仅当两个权限都已授予时显示），点击才启动 `DynamicIslandSettingsActivity`，并立即内存标记+持久化 `onboarded=true`。
- 触发点由相机页自动改为引导页（授权页）手动「下一步」，符合「仅引导时触发」。
- 编译通过：`assembleDebug` BUILD SUCCESSFUL，产物 app-debug.apk（23M，19:54）。

## 20:36 修复 effects 面板编译错误 + 完成深色适配/调色盘重构/对焦快门轨道
- 续前一轮（429 中断）：EffectsPanel 三个内部可组合缺 isDark 形参（ColorSquare/TintSelector/FilterBtn 调用已传 isDark，定义未加），导致无法编译；本次补齐形参并完善深色适配。
- ColorSquare：无滤镜(NONE)改为纯浅灰 flat 色 + 仅暖/冷叠加垂直暗化层；enabled 仍按 tintActive（无滤镜不可操控）。
- TintSelector：加 isDark（背景 RetroDarkSurface/浅灰）、文字白/棕；新增 pointerInput 横向拖拽→循环切换三态（阈值24px）；TintArrow 加 color/bg 参数。
- FilterBtn/FilterSwitch/SlideHintBubble：均加 isDark，药丸底/开关轨/进度气泡在深色用深灰面+浅灰进度/白字。
- CameraScreen：EffectsPanel 调用补 isDark=isDark。
- ControlBar.CameraParamItem（对焦距离/快门速度）：新增横向进度轨道（深色底0xFF3A3A3A、进度0xFFCFCFCF；浅色底0xFFE0DCD4、进度 RetroRust），满足“深色进度背景浅灰”。
- 编译通过：`assembleDebug` BUILD SUCCESSFUL，产物 app-debug.apk（23M，20:36）。

## 20:48 三处微调：灵动岛描边更深 / TintSelector去背景写死宽度 / 隐藏禁用强度滑块
- ① 深色模式灵动岛描边更深：`CaptureTransitionOverlay.kt` 新增 `IslandBorderDark = Color(0xFF5A5A5A)`（比原 `Color.Gray` 0x888888 深），替换两处（morph 岛 shellLine 340、grow 后常驻岛 piLine 402）`if(isDark) Modifier.border(1.dp, IslandBorderDark, ...)`。
- ② TintSelector 去背景、中间文字写死宽度（`EffectsPanel.kt`）：移除 Row 容器 `.background(RetroDarkSurface/浅灰)`（仅 `<>` 箭头 TintArrow 保留圆背景 `arrowBg`）；中间滤镜名 `Text` 包进 `Box(Modifier.width(64.dp), contentAlignment=Center)` + `TextAlign.Center` → 控件整体宽度不随「无滤镜/暖色/冷色」文字长短变化。
- ③ 修复"奇怪的进度条"：`EffectsPanel.kt` 左列强度 `Slider` 在 `TintState.NONE`（无滤镜）时为 `enabled=false` 且 `value=1f` → Material3 渲染成一条满铺灰条（像进度条）。改为 `if(tintActive)` 才渲染 Slider（enabled=true），否则用 `Spacer(sliderHeight+8.dp)` 占位（避免下方 TintSelector 在 NONE/有滤镜间切换时上下跳动）。注：对焦距离/快门速度的轨道是用户上一轮明确要的滑块（保留，非本次 bug）。
- 编译通过：`assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk（约 24MB，20:48）。

## 21:03 修复：对焦距离/快门速度中间的"奇怪进度条"
- 用户纠正：上一轮（20:48）误把 EffectsPanel 强度 Slider 当成因，实际"奇怪进度条"位于**对焦距离与快门速度的 CameraParamItem 行中间**（非效果面板）。
- 根因：`ControlBar.kt` 的 `CameraParamItem` 在 20:36 轮新增的 4dp 横向轨道（`Box(align Center).fillMaxWidth().height(4.dp)`，进度 `fillMaxWidth(fraction)`）。默认 `focusFraction=shutterFraction=0f` → 轨道为一条无滑块、无填充的裸灰横条，落在两个参数项之间，看起来像多余的进度条。
- 修复：直接删除该静态轨道 `Box`（含 progress 子 Box 两行）。对焦/快门本就是「文字 + < > 横滑调节」交互，删除后回归清爽设计；深色适配已由文字色、SlideHintIcon 色、拖拽时 ParamHintBubble（均 isDark 分支）覆盖，不丢失。
- 编译通过：`assembleDebug` BUILD SUCCESSFUL，产物 app/build/outputs/apk/debug/app-debug.apk（约 24MB，21:03）。

## 21:19 四处修复：调色盘对齐 / 设置页杂项组 / 触发链条回归 / 缩略图置左去圆角
- 用户四点反馈：① 调色盘顶部与黑白/暗角/亮度列表顶部未对齐；② 设置末尾"反馈与Bug提交"删除，新增"杂项"组（反馈+查看最新版本）；③ 每次进相机都触发设置（触发链有bug），应仅在引导界面才触发；④ 照片详情页底部缩略图当前照片应在最左而非居中、且不用圆角。
- ① `EffectsPanel.kt`：左侧调色盘 Column 移除多余 `top = 8.dp` 内边距（`Modifier.padding(start = edgeInset)` 不含 top），与右侧滤镜列表顶部对齐。
- ② `SettingsActivity.kt`：删除「个性化」组内独立的"反馈与Bug提交"按钮；在 Developer 组后新增第五组「杂项」，含两项圆角卡片——「反馈与Bug提交」（跳转预留链接）和「查看最新版本」（Toast 显示当前版本号 V1.1，用 `context.packageManager.getPackageInfo` 动态读取）。新增 `Toast` import 和 versionName 变量。
- ③ `CameraScreen.kt`：**关键 bug 回归修复**。代码已回退到 `LaunchedEffect(Unit)` 自动启动灵动岛/取景框设置（每次进相机都触发）。改为：仅当 `权限齐全 && onboarded==true` 时显示 CameraContent；否则走 PermissionRequest（引导页），由新增药丸型「下一步」按钮手动触发 DynamicIslandSettingsActivity + 持久化 `onboarded=true`。符合"仅引导界面触发"，相机页面本身不参与。
- ④ `PhotoViewerActivity.kt`：底部缩略图 LazyRow 改为 `Arrangement.Start` + `contentPadding(horizontal=24dp)`（配合自动滚动居中使当前照片落在最左）；ThumbItem 去掉 `RoundedCornerShape(8.dp)` 圆角和 `.clip()`，border shape 改 `RoundedCornerShape(0.dp)`（RectangleShape 在本 Compose BOM 不可用）。
- 编译坑：首轮 `RectangleShape` 未解析（BOM 2025.09.00 无此符号）→ 改 `RoundedCornerShape(0.dp)`；SettingsActivity 缺 `Toast` import → 补充。二修后编译通过。
- 编译通过：`assembleDebug` BUILD SUCCESSFUL in 40s，产物 app-debug.apk（约 24MB，21:22）。

## 21:35 第三轮反馈：效果面板滑块常显 / 杂项组缺失根因 / 主题系统弹窗 / 两设置页弹窗深色 / 缩略图置左
- 用户第三轮（继 21:19）反馈：① 效果面板调色盘下强度滑块无滤镜时也要出现（灰显不可操作）；② 设置"杂项"组仍看不到；③ 主题颜色改用 Material 系统单选弹窗并适配深色；④ 灵动岛/取景框设置页"还原默认"二次弹窗适配深色；⑤ 照片详情当前照片仍不在最左。
- ① `EffectsPanel.kt`：左列强度 Slider 由 `if(tintActive){Slider} else {Spacer}` 改为**始终渲染**，仅 `enabled = tintActive`；补 `disabledThumbColor/disabledActiveTrackColor/disabledInactiveTrackColor` 灰显（深/浅各一组）。无滤镜时显示一条置灰不可拖的滑块，两态高度一致不跳动。
- ② 根因：`SettingsActivity` 的 `SettingsContent` 内部 `Column` 用 `fillMaxSize()` **且无 `verticalScroll`** → 内容高于屏时最底部「杂项」组被裁切不可见（用户所谓"没添加"实为被截断）。修法：该 Column 加 `verticalScroll(rememberScrollState())`（新增 `rememberScrollState`/`verticalScroll` import），五组均可滚到。
- ③ `SettingsActivity.kt`：`ThemeColorRow` 由行内小菜单改为 **整行点按弹出 Material `AlertDialog`**（单选列表，`RadioButton` × 3：浅色/深色/跟随系统，选中即生效）；弹窗 `containerColor = surfaceCard(isDark)`（适配深色），标题/选项文字 `onSurface(isDark)`，取消 `TextButton`，单选圆点 `selectedColor = if(isDark) RetroCream else RetroBrown`（深模奶油白保证对比）、`unselectedColor = onSurfaceSoft(isDark)`。新增 import：AlertDialog/RadioButton/RadioButtonDefaults/TextButton。
- ④ `DynamicIslandSettingsActivity.kt` + `ViewfinderSettingsActivity.kt`：两页"还原默认"`AlertDialog` 均加 `containerColor = surfaceCard(isDark)`（容器随深色切换深灰/奶油白），确认「还原」文字色改 `RetroRust`（原 `onSurface` 白在浅色奶油底上不可读）。（项目 `PicocamTheme` 固定 `lightColorScheme`，深色由 `isDark`+`onSurface/surfaceCard` 手动处理，故 M3 组件必须显式设 containerColor 才不渲染成浅色。）
- ⑤ `PhotoViewerActivity.kt`：缩略图置左——`LaunchedEffect(photos)` 在 `pagerState.scrollToPage(idx)` 后补 **`thumbListState.scrollToItem(idx)`**（瞬时定位，避免从首屏动画过渡短暂"居中"观感）；`LaunchedEffect(currentIndex)` 保留 `animateScrollToItem(currentIndex)`（翻页时动画置左）。配合 21:19 的 `Arrangement.Start`+`contentPadding(24dp)`，当前照片稳定落在最左。
- 编译坑：首轮 `SettingsActivity` 缺 `TextButton` import → 补后二修 `assembleDebug` BUILD SUCCESSFUL（仅若干 deprecation warning），产物 app/build/outputs/apk/debug/app-debug.apk（约 24MB，21:35）。
- 待真机验证：① 无滤镜时强度滑块灰显不可拖；② 设置页下滑可见「杂项」组；③ 主题弹窗在浅/深模式下容器与文字均正确；④ 两设置页还原弹窗深模为深灰底；⑤ 详情页当前照片位于缩略图最左。

## 21:50 四轮修复：组序交换 / 详情页全页滑+缩略图距左 / 引导页布局 / TintSelector <>logo+示意
- 用户四点：① "杂项"组上移，放到 Developer 上面；② 照片详情页左右滑动判定太小（应整个页面可滑）；缩略图当前照片改为距左侧有一定距离而非最左；③ 引导页权限全获后"下一步"按钮遮住了中间示意；④ 效果面板调色盘下 TintSelector 右侧加 `<>` logo，横滑时上部弹出滤镜选择示意。
- ① `SettingsActivity.kt`：将第五组「杂项」整体移**到第四组「Developer」之前**（即顺序：灵动岛→动画→个性化→**杂项**→Developer），两组代码块互换。
- ② `PhotoViewerActivity.kt` — 双修：
  - **全页滑动**：`ZoomablePhoto.transformable` 加 `enabled = scale > 1.001f`（未放大时禁用 transformable，不消费手势，横滑全交 `HorizontalPager` 切换照片）；新增双击缩放（`pointerInput` + `detectTapGestures(onDoubleTap)` 切换 1x↔2.5x）。新增 import：`detectTapGestures`、`pointerInput`、`LocalDensity`。
  - **缩略图距左**：`scrollToItem(idx, scrollOffset)` / `animateScrollToItem(currentIndex, scrollOffset)` 带正偏移（40dp 转 px），当前照片距左有一定间隙，前一张在左侧微露（胶片条感，而非硬贴最左）。
- ③ `CameraScreen.kt` — PermissionRequest：将"下一步"按钮 (`if(hasCamera && hasLocation)` 块) 从**定位授权 Box 内部**移出，改为**独立放在定位 Button 下方**（两权限按钮后），间距 24dp，避免遮住中间示意。
- ④ `EffectsPanel.kt` — 三改：
  - **TintSelector 新增 `<>` logo**：右侧加 `Spacer(6dp) + SlideHintIcon()`，提示该区域可横滑切换滤镜。
  - **横滑示意气泡**：新增 `tintDragging/tintBtnPos/tintBtnWidth` 状态；TintSelector 添加 `onGloballyPositioned` 上报位置，`detectHorizontalDragGestures` 获 `onDragStart/onDragEnd/onDragCancel` 驱动 tintDragging；新增第二 `AnimatedVisibility`（与滤镜 hint 同级）在上方弹出 `TintHintBubble`（与滤镜按钮同形同大，显示当前滤镜名 + < > 方向）。
  - **TintHintBubble** 新增 composable：深/浅自适应背景，`< 无滤镜 >` 或 `< 暖色 >`、`< 冷色 >`。新增 TintSelector 参数：`onDragHintStart`/`onDragHintEnd`/`onBtnLayout`。
- 编译：`assembleDebug` BUILD SUCCESSFUL（仅若干已有 deprecation warning），产物 app-debug.apk（约 24MB，21:50）。

## 22:05 四小修复：箭头z序/Developer去标签 / TintSelector图标+比例映射 / 引导页自动跳 / 两设置页返回统一
- 用户四点：① 设置左上箭头要在最上层；Developer 区 GitHub 图标不正常、"作者""特别鸣谢"去掉名字居中；② TintSelector 的 `<>` 图标定位溢出、拖拽映射不对（应在半屏内滑完所有滤镜）；③ 引导页去掉"下一步"按钮、权限全获后自动进下一步；④ 从设置进入的灵动岛/取景框调整页，左侧返回按钮样式应与设置页一致。
- ① `SettingsActivity.kt`：返回按钮 Box 挪到 `Column` **之后**（画在上层，点击优先响应）；Column 左/上 padding 改为 `start=56.dp, top=84.dp`（避开按钮覆盖范围）。Developer 区：Genkaim/Zczzzzz 各自删除 "作者" / "特别鸣谢"副文本（名字居中、weight(1f)填充）。GitHub logo 维持原 GithubLogo 绘制（路径 `GITHUB_PATH` 于 `Canvas(24dp) drawPath`）。
- ② `EffectsPanel.kt`：**`<>` 图标溢出**——去掉 `Spacer(6dp)`、padding 从 `horizontal=14dp` 改为 `start=10dp, end=6dp`（总宽 164dp 下 32+64+32+16=144dp ≤ 148dp 可用，不溢出）。**拖拽映射**——从阈值型（24px 跳跃单态）改为**累积比例型**：`stepPx = 60dp.toPx()`，`onHorizontalDrag` 中按 `dragAccum/stepPx` 槽位变化量 `diff` 逐次循环（`repeat(diff) onCycle`），半屏横滑约 180dp = 3 步 → 遍历全部三态。新增 `val density = LocalDensity.current`。
- ③ `CameraScreen.kt` — `PermissionRequest`：删除 `if(hasCamera && hasLocation)` 内 `Spacer(24dp) + Box("下一步")`，替换为 `if(hasCamera && hasLocation) LaunchedEffect(Unit) { delay(800L); onNext() }`（权限齐全后 800ms 自动进入灵动岛设置，无需手动点下一步）。
- ④ `DynamicIslandSettingsActivity.kt` + `ViewfinderSettingsActivity.kt`：底部面板中删除 `if(!onboarding) Box(40dp,RoundedCornerShape(20dp))`（原内联返回），改为**浮动 TopStart 返回按钮** —— `align=TopStart, padding(8dp,8dp), size(44dp), RoundedCornerShape(22dp), surfaceCard` + backdrop 图标，与设置页完全一致。header Row `start` padding 自 24dp 改为 60dp（为浮动按钮让位）。
- 编译：`assembleDebug` BUILD SUCCESSFUL（零新错），产物 app-debug.apk（约 24MB，22:05）。
- 编译：`assembleDebug` BUILD SUCCESSFUL（仅若干已有 deprecation warning），产物 app-debug.apk（约 24MB，21:50）。

## 22:15 六小修复：内容padding还原/拖拽步长/<>位置/返回按钮回面板/文件名InstCam/标题收缩+顶部渐变
- 用户六点：① 设置返回按钮不该占单独一列（内容左边空出）；② TintSelector 拖拽映射太长，半屏内应完成所有切换；③ `<>` 图标应在滤镜名字后面而非右箭头后面；④ 两设置页返回按钮应在可调节区域内而非全屏左上角；⑤ 保存照片文件名 PICOCAM→InstCam；⑥ 设置标题滚动上移后移到返回按钮右侧，顶部加背景→透明渐变。
- ① `SettingsActivity.kt`：Column padding 从 `start=56dp, top=84dp` 还原为 `horizontal=24dp, vertical=72dp`（返回按钮浮动在上方不影响内容排版）。
- ② `EffectsPanel.kt`：`stepPx` 60dp→**40dp**（半屏~160dp / 40dp = 4步，遍历全部三态有余）。
- ③ `EffectsPanel.kt`：TintSelector Row 内容顺序从 `[<] [name] [>] [<>]` 改为 `[<] [name] [<>] [>]`（`<>` 图标在名字与右箭头之间）。
- ④ `DynamicIslandSettingsActivity.kt` + `ViewfinderSettingsActivity.kt`：撤回浮动 TopStart 返回按钮，改回**底部面板 header Row 内**（44dp / RoundedCornerShape(22dp) / surfaceCard，与设置页一致），Row padding `start=8dp`。
- ⑤ `camera/PhotoStorage.kt`：`generateFile` 中 `"PICOCAM_..."` → `"InstCam_${timestamp}.jpg"`。
- ⑥ `SettingsActivity.kt`：新增 `scrollState` 变量 + `titleCollapse = derivedStateOf { scrollState.value/120f coerceIn(0,1) }`。大标题 `Text("设置")` 加 `alpha(1f - titleCollapse)`（滚动时淡出）；新增**顶部渐变** `Box(height=72dp, Brush.verticalGradient(pageBg → transparent))`；新增**收缩标题** `Text("设置", titleLarge, alpha=titleCollapse, padding(start=64dp, top=14dp))` 在返回按钮右侧淡入。新增 import：`derivedStateOf`/`Brush`/`alpha`。
- 编译：`assembleDebug` BUILD SUCCESSFUL（零新错零新 warn），产物 app-debug.apk（约 24MB，22:15）。

## 22:26 八微调：引导标题/缩略图出屏/照片上移/特别鸣谢标签/GitHub放大/<>颜色+间距/延迟减半
- 用户八点：① 引导进入的灵动岛/取景框标题太靠左；② 缩略图当前照片定位超出屏幕；③ 照片详情页照片位置稍微网上视觉平衡；④ Zczzzzz 前加和 Developer 样式一样的「特别鸣谢」字样；⑤ Genkaim 后 GitHub 图标太小；⑥ `<>` 图标透明度增大 + 与右箭头增加距离；⑦ 引导页权限全获后延迟降低；⑧ 拍照延迟降低。
- ① `DynamicIsland/ViewfinderSettingsActivity.kt`：header Row padding 改为 `start = if(onboarding) 24.dp else 8.dp`（引导模式 24dp 避免贴边；正常模式 8dp 容纳 44dp 返回按钮）。
- ② `PhotoViewerActivity.kt` — 缩略图出屏根因：`scrollToItem(idx, positiveOffset)` 会**裁掉**照片左侧导致看似出屏（Compose 语义：正 offset 把首项向上/左滚出可视区）。改用 `contentPadding(start=48dp, end=24dp)` 制造"距左距离"（前面照片在左侧微露），`scrollToItem(idx)` 与 `animateScrollToItem(idx)` 全部用 offset=0。
- ③ `PhotoViewerActivity.kt` HorizontalPager page padding 从 `24dp` 改为 `start=24dp, end=24dp, top=12dp, bottom=36dp`（上小下大，照片视觉中心略偏上，视觉平衡）。
- ④ `SettingsActivity.kt` Developer 段 Zczzzzz 上方加 `GroupTitle("特别鸣谢", isDark) + Spacer(10.dp)`（与 Developer 段同款小字、细字重、淡色样式）。
- ⑤ `SettingsActivity.kt` GitHub 容器 40dp→48dp，GithubLogo 内部 Canvas 24dp→32dp。
- ⑥ `EffectsPanel.kt` TintSelector：新增 `hintColor = if(isDark) Color.White else RetroBrown`（比箭头颜色更不透明），`SlideHintIcon(color = hintColor)`；`<>` 与右箭头之间加 `Spacer(Modifier.width(6.dp))`；Row padding 调整为 `start=8dp, end=4dp` 保持 164dp 容器不溢出。
- ⑦ `CameraScreen.kt` PermissionRequest：`delay(800L)` → `delay(400L)`。
- ⑧ `CameraScreen.kt` 拍照过渡：`delay(300L)` × 2 → `delay(150L)` × 2；`shutterFlash.animateTo(0f, tween(300))` → `tween(150)`，白闪时间减半。
- 编译：`assembleDebug` BUILD SUCCESSFUL（仅若干已有 deprecation warning），产物 app-debug.apk（约 24MB，22:26）。
